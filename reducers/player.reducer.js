import { playerConstants } from '../constants';

// initial player state
const initialState = {};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}
